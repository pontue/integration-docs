{
  "name": "pontue-integration-docs",
  "version": "2.1.0",
  "dependencies": {
    "@scalar/api-reference": "1.72.1"
  },
  "private": true,
  "scripts": {
    "start": "scalar document serve openapi/openapi.yaml --watch --port 8082",
    "build": "node scripts/build.mjs",
    "preview": "npm run build && serve dist --listen 8082",
    "test": "scalar document validate openapi/openapi.yaml"
  },
  "devDependencies": {
    "@scalar/cli": "2.5.2",
    "serve": "14.2.6"
  },
  "engines": {
    "node": ">=24"
  }
}
